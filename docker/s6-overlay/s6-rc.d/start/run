#!/command/execlineb -P

# Use with-contenv to ensure environment variables are available
with-contenv

# Change to the application directory
cd /var/www/html

# Start Octane server with proper variable handling
foreground {
    php
    artisan
    octane:start
    --server=swoole
    --host=0.0.0.0
    --port=8000
}